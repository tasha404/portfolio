export default function Navbar() {
  return (
    <nav className="navbar">
      <h2 className="logo">Natasha</h2>
      <div>
        <a href="#">About</a>
        <a href="#">Projects</a>
        <button>Contact</button>
      </div>
    </nav>
  );
}